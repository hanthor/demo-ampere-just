[tool.poetry]
name = "demo-ampere-just"
version = "0.1.0"
description = ""
authors = ["James <your-email@example.com>"]

[tool.poetry.dependencies]
python = "^3.8"
rich = "^10.0.0"
pygments = "^2.9.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"